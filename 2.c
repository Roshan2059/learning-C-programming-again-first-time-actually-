#include<stdio.h>

void main(){
	int a, b, result;
	printf("Enter the value of a:\n");
	scanf("%d", &a);
	printf("Enter the value of b:\n");
	scanf("%d", &b);
	result = a + b;
	printf("If we add those two numbers we get the result: %d", result);
}
